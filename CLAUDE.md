# CRM-ECO

Read `CONTEXT.md` for domain vocabulary. Production writes follow `.cursor/rules/production-change-playbook.mdc`.

## Supabase — PIFH is connected (2026-09-01)

The CLI is the **PIF Ecosystem** account. This repo is linked to production **PIF-ECO-V2**.

- Org: `khhfyzojnmmjplgeroug` (PIF Ecosystem)
- Project: `sffisarikcreyyjzdjvb` (PIF-ECO-V2) — ● LINKED
- Confirm with `supabase projects list` if the session looks stale
- Omnivurse is a different account. Do not use it here
- Cursor user-level Supabase MCP may still list Omnivurse; the workspace PIFH MCP is the live one
- Do not apply migrations or write production without explicit owner approval
