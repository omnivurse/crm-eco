# Live reality — 2026-08-22 (Road to Ten, Wave 0)

> Two sections: **Prod reality (RO-1)** — the read-only PIFH prod precondition
> table (config from `scripts/e2e/fixture-shapes.json`, live COUNTs via
> `scripts/audit-crm-ux-reality.mjs`; regenerate with
> `node scripts/audit-crm-ux-reality.mjs --write`, or
> `node scripts/audit-crm-visibility.mjs --ux-reality --write`) — and **Local
> fixture (EV-2)** — what the walk harness seeds locally. No record data, no PII.

## Prod reality (RO-1)

<!-- RO-1:begin (generated by scripts/audit-crm-ux-reality.mjs) -->
> Generated by `node scripts/audit-crm-ux-reality.mjs` — config from `scripts/e2e/fixture-shapes.json`
> (captured 2026-08-23T01:39:50.592Z), live counts from sffisarikcreyyjzdjvb.supabase.co at 2026-08-23T02:25:16.606Z. Counts and config only; no record values, no PII.

| # | Precondition | Verdict | Prod value |
|---|--------------|---------|------------|
| 1 | crm_layouts contacts: one is_default row, one hero section, collapsed set | PASS | 2 row(s), 1 default ("Default Contact Layout", 27 sections); hero=core; open: core, health_sharing, insurance, health_insurance, address, management, identifiers; collapsed(20): main, personal, contact, notes_history, dental_coverage, vision_coverage, other_coverage, life_coverage, family_spouse, family_children, relationships, payment, portal, compliance, fulfillment, business, preferences, commissions, conversion, system — management/insurance/identifiers collapsed=false |
| 1 | crm_layouts leads: one is_default row, one hero section, collapsed set | PASS | 2 row(s), 1 default ("Default Lead Layout", 19 sections); hero=core; open: core, health_sharing, health_insurance; collapsed(16): main, personal, notes_history, dental_coverage, vision_coverage, other_coverage, life_coverage, product, family, relationships, address, management, compliance, preferences, conversion, system |
| 1 | crm_layouts members: one is_default row, one hero section, collapsed set | PASS | 1 row(s), 1 default ("Default Member Layout", 13 sections); hero=core; open: core, health_sharing, insurance, coverage, health_insurance; collapsed(8): main, dental_coverage, vision_coverage, other_coverage, life_coverage, address, advisor, management |
| 1 | crm_layouts other modules (non-people; fail-open per DynamicRecordForm) | INFO | enrollment_approval_test: 0 layout(s), no default; advisors: 0 layout(s), no default; deals: 1 layout(s), default "Default Layout" hero=no; accounts: 1 layout(s), default "Default Layout" hero=no; prospects: 1 layout(s), default "Default Layout" hero=no |
| 2 | crm_views enrollment_approval_test default | INFO | no views |
| 2 | crm_views contacts default view: sort + columns | PASS | 1 view(s), 1 default ("All Contacts" 74bc3abf); sort=created_at desc; columns=first_name, last_name, email, phone, contact_status, mailing_city, product, producer_name |
| 2 | crm_views leads default view: sort + columns | PASS | 1 view(s), 1 default ("All Leads" 550c7102); sort=created_at desc; columns=first_name, last_name, email, phone, lead_status, lead_source, city, producer |
| 2 | crm_views members default view: sort + columns | PASS | 1 view(s), 1 default ("All Members" fdf7ae2b); sort=created_at desc; columns=first_name, last_name, member_number, contact_status, plan_name, effective_date, city, referral, advisor_name, phone |
| 2 | crm_views advisors default | INFO | no views |
| 2 | crm_views deals default view: sort + columns | INFO | 1 view(s), 1 default ("All Members" ddce96c1); sort=created_at desc; columns=member_name, email, phone, stage, enrollment_date, plan |
| 2 | crm_views accounts default view: sort + columns | INFO | 1 view(s), 1 default ("All Accounts" 394fa8a1); sort=created_at desc; columns=account_name, website, phone, industry, employees, billing_city |
| 2 | crm_views prospects default view: sort + columns | INFO | 5 view(s), 1 default ("All Prospects" 91b9782c); sort=created_at desc; columns=first_name, last_name, email, phone, prospect_status, carrier, mailing_city, mailing_state |
| 3 | members fields plan_name / effective_date / product present? | PASS | plan_name: text (section coverage, label "Plan"); effective_date: date (section coverage, label "Effective Date"); product: MISSING; sharing_effective_date: date (section health_sharing, label "Effective Date") — LS-4 must read plan_name (members carry no product field) |
| 3 | contacts.product + leads.product_type: type + option counts (DE-1) | PASS | contacts.product: text, 0 options, label "Membership / Plan", section insurance; leads.product_type: text, 0 options, label "Membership / Plan", section product; contacts.product_type (second field): text, 0 options, label "Product Type" |
| 3 | health_insurance_plan_name type (DE-2/D4 free text + suggestions) | PASS | contacts: text/0 options; leads: text/0 options; members: text/0 options |
| 3 | producer display keys (DE-3 / D5 adjusted) | INFO | contacts.producer_name: text (management); contacts.producer_record_id: MISSING; leads.producer: text (management); members.advisor_name: text (advisor) |
| 4 | advisor store counts (crm_advisors / crm_records advisors / public.advisors) | PASS | crm_advisors: 0 total / 0 active; crm_records 'advisors' (not deleted): 18; public.advisors (org-scoped): 672 total / 672 is_active / 672 not deleted — picker source of truth = public.advisors (D5 adjusted) |
| 5 | crm.layout.v2 global + org; crm.nav.simple org | PASS | crm.layout.v2 global=true, PIFH=no row (inherits global); crm.nav.simple PIFH=false, global=no row → full shell |
| 6 | crm_modules enabled + field counts → sidebar roster | PASS | enrollment_approval_test off (0 fields, 0 records); contacts ON (280 fields, 14,144 records); leads ON (169 fields, 1,127 records); members ON (94 fields, 1,063 records); advisors ON (10 fields, 18 records); deals off (7 fields, 0 records); accounts ON (7 fields, 1 records); prospects off (106 fields, 0 records) → navigable (nav-profile.navigableModules): Sales Pipeline contacts, leads, members, accounts; People Management → Advisors shown; deals/prospects not navigable (NV-2 redirect) |
| 7 | contacts: allowed status values + per-lane counts | PASS | allowed (11): Active, Inactive, Pending, In Process, Cancelled, Terminated, Deceased, Prospect, Lost, Declined, Abandoned; 14,076 live rows, 10 distinct raw spellings, 2 blank/null, 14,074 (100%) already in vocabulary; lanes: active 5,751 (1 spellings) · pending 45 (1 spellings) · in_process 18 (1 spellings) · new 971 (1 spellings) · inactive 489 (1 spellings) · cancelled 6,800 (5 spellings) · other 2 (0 spellings) |
| 7 | leads: allowed status values + per-lane counts | PASS | allowed (10): New, Attempted, Contacted, Qualified, Future Prospect, In Process, Pending, Converted, Unqualified, Lost; 1,127 live rows, 9 distinct raw spellings, 2 blank/null, 1,125 (100%) already in vocabulary; lanes: active 0 (0 spellings) · pending 10 (1 spellings) · in_process 273 (1 spellings) · new 59 (1 spellings) · inactive 0 (0 spellings) · cancelled 30 (2 spellings) · other 755 (4 spellings) |
| 7 | members: allowed status values + per-lane counts | PASS | allowed (11): Active, Inactive, Pending, In Process, Cancelled, Terminated, Deceased, Prospect, Lost, Declined, Abandoned; 1,062 live rows, 3 distinct raw spellings, 0 blank/null, 1,062 (100%) already in vocabulary; lanes: active 998 (1 spellings) · pending 1 (1 spellings) · in_process 0 (0 spellings) · new 0 (0 spellings) · inactive 63 (1 spellings) · cancelled 0 (0 spellings) · other 0 (0 spellings) |

Verdicts: 15 PASS · 0 FAIL · 7 INFO.
<!-- RO-1:end -->

### RO-1 read-out — rows that contradict / sharpen the plan's assumptions

Hand-written from the table above (the table block is regenerated; this list is not).

| Row | Plan / decision assumption | Prod reality | Consequence |
|-----|----------------------------|--------------|-------------|
| 3 | DE-1 asked "contacts.product / leads.product_type options populated?" | Both are `text` with **0 options** (free text), as is the second `contacts.product_type` field | DE-1 must ship the option list (census tier-A 43, `scripts/e2e/product-options.proposed.json`) — there is no picklist to reuse; values stay as stored (additive options only) |
| 3 | LS-4 "members plan/product" | `members.product` field does **not exist**; `members.plan_name` (text, section coverage) + `effective_date` (date) exist and are columns of the "All Members" default view — but the census found no plan text in members crm_records | LS-4 reads `plan_name`/`effective_date` for members and must render the empty state honestly; do not build anything on `members.product` |
| 3 | D5 (adjusted): write `producer_record_id` alongside `producer_name` | No `producer_record_id` crm_fields row on contacts (or leads/members); display keys are `contacts.producer_name`, `leads.producer`, `members.advisor_name` | DE-3 needs an additive hidden/system field (or a data key without a crm_fields row — decide explicitly) before the picker can persist the id; three different display keys, not one |
| 4 | Original D5: CRM advisors module / crm_advisors as picker source | `crm_advisors` 0 rows; crm_records 'advisors' 18 rows; `public.advisors` 672 (all active, none deleted) | Confirms the orchestrator's D5 adjustment: picker source of truth = `public.advisors` scoped by `organization_id` |
| 1/2/6 | "advisors navigable?" | advisors module is ON with 10 fields and 18 records but has **0 layouts and 0 views** (fail-open DynamicRecordForm; list falls back to module defaults) | NV items: /crm/advisors is reachable from People Management with no saved view and no layout — either seed a default view/layout or expect the fallback UI on the walk |
| 2 | Default sort truth for TE-3 | All three people default views sort `created_at desc`; columns as listed (members: plan_name, effective_date, advisor_name included) | TE-3 can assume created_at desc everywhere; no per-module surprise |
| 2 | — | Disabled `deals` module's only view is named "All Members" (ddce96c1) | Cosmetic/INFO; module is not navigable (NV-2 redirect), nothing to do this wave |
| 7 | Lane chips (LS-*) assume statuses bucket into lanes | contacts: 100% in vocabulary, `other` = 2 blank rows only; members: 100%, 3 spellings; **leads: 755 of 1,127 (67%) land in `other`** (4 spellings — the open pipeline words Attempted/Contacted/Qualified/Converted by design, see status-lanes.ts) | Leads lane chips are mostly "Other"; if LS lane work targets /crm/leads, either accept it or give leads a pipeline-lane set — a decision, not a defect |
| 7 | Wave-0 defect: status-values RPC 500s | The RPC works when jsonb params are passed as arrays/objects (this script uses that shape and got all three per-module groupings) | Confirms the fix shape for status-lanes.ts:266-270 / reports execute route |
| 5 | D10: full shell (no simple nav) + layout V2 | `crm.layout.v2` global=true (no org row), `crm.nav.simple` PIFH=false | As assumed |

## Local fixture (EV-2)

The walk harness runs against the LOCAL Supabase stack only
(`supabase db push` / `db reset` target PROD in this repo and are never used).
Three scripts under `scripts/e2e/` make the local stack a PIFH-shaped,
idempotent walk fixture:

| Script | What it does | Guard |
|---|---|---|
| `apply-local-migrations.sh` | Applies every `supabase/migrations/*.sql` newer than `max(schema_migrations.version)` and absent from the ledger, one `psql --single-transaction` per file, inserting the ledger row on success; failures roll back and are reported (`already exists`/`duplicate key` ERRORs → "already-present"). `--dry-run`, `--strict`, `--mark <version>` (record a ledger row for a file verified to be in effect, no SQL run). | refuses any DB host that is not 127.0.0.1/localhost (exit 2) |
| `dump-prod-fixture-shapes.mjs` | READ-ONLY snapshot of PIFH CRM config on prod → `fixture-shapes.json` (crm_modules, crm_fields, crm_views, crm_layouts, crm_feature_flags, crm_status_vocabulary + aggregate counts). Service-role key from env / `apps/crm/.env.local` / `.env`, never printed. No records, no PII. | read-only SELECTs |
| `seed-walk-fixture.mjs` | Idempotent seed (service-role supabase-js + psql): 3 auth users + profiles, feature flags (PIFH `crm.nav.simple=false`, asserts global `crm.layout.v2`), prod-mirrored modules/fields/views/layouts from `fixture-shapes.json`, fixture records (statuses pre-checked against `crm_status_vocabulary`), one legacy note; then a verification block incl. the real anon-key login path for all three users. `--verify-only` runs only the checks. | refuses any Supabase URL or DB URL not on 127.0.0.1/localhost (exit 2); never deletes |

`scripts/seed-local.mjs` (legacy, no localhost guard, prod-capable) was removed —
there is one local seeder now.

### Usage
```
# local stack already running (supabase start was done elsewhere; never run it from a worktree)
scripts/e2e/apply-local-migrations.sh          # bring the local ledger up to the repo
node scripts/e2e/seed-walk-fixture.mjs         # seed + verify (idempotent; re-run any time)
node scripts/e2e/seed-walk-fixture.mjs --verify-only
# refresh the prod config snapshot (read-only): node scripts/e2e/dump-prod-fixture-shapes.mjs
```
Defaults: API `http://127.0.0.1:54321`, DB `postgresql://postgres:postgres@127.0.0.1:54322/postgres`,
local demo service-role/anon keys. Override with `LOCAL_SUPABASE_URL`, `LOCAL_SERVICE_ROLE_KEY`,
`LOCAL_ANON_KEY`, `LOCAL_DB_URL`. Pointing either URL at prod exits 2 before any network call
(verified: `LOCAL_SUPABASE_URL=https://sffisarikcreyyjzdjvb.supabase.co node scripts/e2e/seed-walk-fixture.mjs` → exit 2).

### Fixture contract (shared with the harness)
| Item | Value |
|---|---|
| operator | `walk-operator@example.invalid` / `Walk-Operator-2026!` — profile role `staff`, `crm_role` `crm_agent`, org `0000…0001`, active |
| admin | `walk-admin@example.invalid` / `Walk-Admin-2026!` — role `admin`, `crm_role` `crm_admin` |
| viewer | `walk-viewer@example.invalid` / `Walk-Viewer-2026!` — role `staff`, `crm_role` `crm_viewer` |
| anchor contact | Wendy Walker · phone `5550107788` (unique **within contacts** — the `members` twin WALK-0001 carries the same phone on purpose; `findAnchorRecord` prefers the contacts row) · `wendy.walker@example.invalid` · member_number `WALK-0001` · status `Active` · product `Sedera Access+` · sharing_entity `Sedera` · sharing_effective_date `2026-09-01` · producer_name `Wen Producer` (+ `producer_record_id` → the advisors record) · one legacy note |
| pending lane | 4 contacts with status `Pending`, created 60/41/23/9 days ago; oldest = **Pat Pending** (`5550107701`) |
| filler | 30 contacts (Active/Inactive/Cancelled/In Process) → 35 contacts total, pages at 25 |
| lead | Lee Lead (`5550107790`, status `New`) |
| advisor | Wen Producer (advisors module, advisor_code `WEN01`) |
| members module | 3 rows (Wendy Walker WALK-0001 Active, Mia Member WALK-0002 Active, Pat Pending WALK-0003 Pending) |
| flags | PIFH `crm.nav.simple=false` (owner decision: full shell); global `crm.layout.v2=true`; PIFH comms pilot flags mirrored from prod |
| PIN gate | cookie `lgq_ok` = future unix-epoch ms (`@crm-eco/ui/lib/pin-lock`) |
| login | `/crm-login` → `#email`, `#password`, `button[type=submit]` |

All fixture records carry `data.walk_fixture = true` and deterministic uuids
(sha1 of a fixed namespace + key), so re-runs update in place. Statuses used:
`Active`, `Pending`, `In Process`, `Inactive`, `Cancelled` (contacts/members) and
`New` (leads) — all in `crm_status_vocabulary` (the DB guard trigger
`crm_0_status_guard_trg` rejects anything else; `Approved Pending` from the
original plan is NOT allowed and was replaced by `Pending`).

### Local DB state after the 2026-08-22 run
- Ledger: 131 → 154 rows, max version `20260820120000` → `20260823003000`.
  22 files applied cleanly; `20260820150000_tighten_bulk_merge_and_age65_guards.sql`
  was already in effect (wrapper marker present — it had been psql-applied without
  a ledger row) and its re-run fails only because `20260820190000` later re-signed
  `private.apply_age_65_auto_cancellation_impl(uuid, uuid)`; recorded with `--mark`.
- `crm_fields`: 123 → 674 (551 prod-shaped rows inserted; 71 pre-existing rows
  aligned to prod label/type/section/options/order; local `members` keeps one
  extra seed.sql field → 95 vs prod 94).
- `crm_modules`: 8 (contacts, leads, members, advisors, accounts enabled; deals,
  prospects, enrollment_approval_test disabled — as prod).
- `crm_views`: 10 (prod ids; one `is_default` per module). `crm_layouts`: 8
  (one default per module; contacts/leads/members defaults carry the `core`
  section with `variant: 'hero'`; contacts `management`/`insurance`/`identifiers` open).
- `crm_records` (PIFH, live): 40 — contacts 35, leads 1, advisors 1, members 3.
  Status spread of fixture rows: Active 18 · Cancelled 5 · In Process 5 ·
  Inactive 5 · New 1 · Pending 5.
- `profiles`: 3 with `crm_role`; `organization_members`: 3 active; all three
  users sign in with the anon key and read their profile row through RLS.

## RO-1 — superseded pointer

The Wave-0 reviewer's snapshot-derived table that used to live here is
superseded by "Prod reality (RO-1)" at the top of this file (same six
preconditions plus status/lane counts, generated by
`scripts/audit-crm-ux-reality.mjs`). Record-value questions (DE-9's
product/producer counts) live in `product-vocabulary-census-2026-08-22.md`.
