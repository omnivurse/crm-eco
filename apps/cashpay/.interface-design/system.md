# Cash Pay interface system

## Direction
Chargemaster night-station. Dual surface: airy marketing on `/`, cockpit instrument on `/search`. Amber is the rate, never decoration. Cool paper / midnight chart; fluorescent hallway steel; ink.

## Density
Marketing: 24–32px section gaps, 16px control pad, 44px targets.
Product: 12–16px rail/ledger pad, 8px tick gaps, sticky chrome 64px, pageSize 50.

## Spacing
Base 4px. Rail/ledger pad 12–16px. Marketing sections `py-24`. Chrome 64px. Compare tray safe-area bottom.

## Type
Ratio 1.25 from 14px body (14 / 18 / 22 / 28 / 35).
Display: Bricolage Grotesque. Body: Plus Jakarta. CPT and money: IBM Plex Mono, tabular-nums.

## Tokens
`--tape` `--ink` `--ink-muted` `--tick` `--hairline` `--signal` `--signal-hover` `--signal-subtle` `--cms-needle` `--focus`
Radius: control 6 · panel 10 · sheet 16.

## Signature
Rate Instrument: facility × CPT tick, loud cash figure (`--signal`), quiet CMS needle, compare tray. Five hits: rail CPT, slice strip, grouped ledger rate, mobile card rate, tray cash.

## Controls
Input 44px · radius 6 · label 11px/600 muted uppercase above.
Primary product: `Read the tape`.
Site CTAs: `Open the instrument` / `License this UI`.
Facility drawer: native dialog, focus trap, Escape, restore focus.
