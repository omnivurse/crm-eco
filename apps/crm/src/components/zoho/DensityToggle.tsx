'use client';

/**
 * Promoted to `@crm-eco/ui/components/density-toggle` so the CRM and Admin
 * consoles share one control (and one token-bound skin — the CRM copy was
 * painted with raw slate/teal classes).
 *
 * Re-exported here so existing `@/components/zoho/DensityToggle` imports keep
 * working unchanged.
 */
export { DensityToggle } from '@crm-eco/ui/components/density-toggle';
