{
  "installCommand": "corepack enable && cd ../.. && corepack prepare pnpm@latest --activate && pnpm install --no-frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm run build:portal",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
