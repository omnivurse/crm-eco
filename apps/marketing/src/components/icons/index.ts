/**
 * Curated Phosphor Duotone icon set used across the marketing site.
 *
 * Centralizing the imports lets us:
 *   1. Tree-shake aggressively (no full-package imports)
 *   2. Keep visual language consistent (one weight: duotone)
 *   3. Swap providers later without scattering imports.
 *
 * Always import from "@/components/icons" — never directly from the package.
 */
export {
  ArrowRight as IconArrowRight,
  ArrowUpRight as IconArrowOut,
  CaretDown as IconCaret,
  Check as IconCheck,
  CheckCircle as IconCheckCircle,
  Sparkle as IconSpark,
  ShieldCheck as IconShield,
  Lock as IconLock,
  Globe as IconGlobe,
  ChartLineUp as IconChart,
  Pulse as IconPulse,
  Lightning as IconBolt,
  Cube as IconCube,
  Stack as IconStack,
  Buildings as IconBuilding,
  Users as IconUsers,
  UserCircleGear as IconUserGear,
  IdentificationCard as IconId,
  Receipt as IconReceipt,
  Wallet as IconWallet,
  Coins as IconCoins,
  ChartPieSlice as IconPie,
  Code as IconCode,
  PlugsConnected as IconPlug,
  Repeat as IconRepeat,
  PaperPlaneTilt as IconSend,
  EnvelopeSimple as IconMail,
  Phone as IconPhone,
  GearSix as IconGear,
  TreeStructure as IconTree,
  Strategy as IconStrategy,
  Heartbeat as IconHeart,
  ListChecks as IconList,
  Database as IconDb,
  Vault as IconVault,
  CursorClick as IconClick,
  Confetti as IconConfetti,
  Star as IconStar,
  Quotes as IconQuote,
  Briefcase as IconBriefcase,
  Brain as IconBrain,
  Funnel as IconFunnel,
  HandCoins as IconHandCoins,
  Eye as IconEye,
  X as IconClose,
  List as IconMenu,
  CaretRight as IconChevronRight,
} from '@phosphor-icons/react/dist/ssr';
