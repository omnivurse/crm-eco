-- ============================================================================
-- PHASE 5: COMMUNICATIONS SCHEMA (DISABLED)
--
-- This migration was disabled because the Portal schema already has
-- email_templates and notification_preferences tables with different structures.
-- The existing tables from Portal migrations are sufficient.
-- ============================================================================

SELECT 1; -- No-op migration
