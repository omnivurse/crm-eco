/*
  # Create match_kb_docs Function for Vector Similarity Search
  
  SKIPPED: Vector extension not reliably available in all Supabase configurations.
  This function can be created manually after migrations if vector search is needed.
  
  To enable later, run:
  1. CREATE EXTENSION IF NOT EXISTS vector;
  2. Then execute the original function definition.
*/

-- No-op migration to unblock the chain
SELECT 1;
