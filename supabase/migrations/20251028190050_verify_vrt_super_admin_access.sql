/*
  # Verify VRT Super Admin Access (Skipped)
  
  SKIPPED because it checks for data that doesn't exist during a fresh reset.
  User data is typically seeded after migrations or created manually.
*/

-- No-op
SELECT 1;
